import "@/features/zamin/zamin.css";
import ZaminPage from "@/features/zamin/ZaminPage";

export const metadata = { title: "Иҷораи замин — AgroSmart.tj" };

// Роҳи "/zamin" — бозори заминҳои иҷора
export default function Zamin() {
  return <ZaminPage />;
}
