import AdminPage from "@/features/admin/AdminPage";

export const metadata = { title: "Панели идора — AgroSmart.tj" };

// Роҳи "/admin" — панели Admin / SuperAdmin
export default function Admin() {
  return <AdminPage />;
}
